# Always & Forever Beauty — Netlify configuration
# The functions folder is the small API layer that talks to Baserow.

[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
